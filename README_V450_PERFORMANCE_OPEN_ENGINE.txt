V450 PERFORMANCE OPEN ENGINE
- Existing documents now show the modal immediately before DB/heavy work.
- Added row cache from dashboard rows to avoid repeated Supabase fetch on document open.
- v364 opener now uses cached row first and shows a small opening badge only while loading.
- No save/status/items logic changed.
