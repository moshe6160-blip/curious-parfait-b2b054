V391 REAL SYNC

- Sync button now pulls directly from Supabase cloud source.
- Manual Sync always re-renders from source, preserves scroll, and shows a status toast.
- Auto refresh checks every 15 seconds and updates only when data changed.
- No page reload and no screen jump.
- Keeps login, push, approvals, and existing code untouched.
